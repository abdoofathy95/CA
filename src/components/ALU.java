package components;

public class ALU {
// must have the following methods
/* ALL of the follwing methods must return a value and have two inputs always
 * MUST ALWAYS CHECK IF THE RESULT IS ZERO BEFORE RETURNING AND SET ZERO SIGNAL TO 1 ( ZERO SIGNAL)
 * add two values and return the sum
 * sub two values and return the sum
 * and 
 * shift left logic by an amount n 
 * shift right logic by an amount n
 * nor 
 * JR (simply return input 1) 
 * Set if less than (return 1 if value1 < value2)
 * Set if less than unsigned (return 1 if value1 < value2 ) ignore the sign bit
 * */
}
